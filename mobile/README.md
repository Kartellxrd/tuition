# Code Ready Tutors mobile
React Native Community CLI + TypeScript client for the Code Ready Tutors FastAPI backend.

The source shell is committed here. Native Android/iOS project folders should be generated with the React Native Community CLI before the first device build; secrets must not be embedded in the app.
