import React,{useState}from"react";import{Text}from"react-native";import Screen from"../../components/Screen";import PasswordInput from"../../components/PasswordInput";import{Button,ErrorText,Input,Muted,Title}from"../../components/UI";import{api}from"../../api/client";import{colors}from"../../theme";export default function RegisterScreen({navigation}){const[name,setName]=useState("");const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[confirm,setConfirm]=useState("");const[busy,setBusy]=useState(false);const[error,setError]=useState("");async function submit(){if(password.length<8){setError("Password must be at least 8 characters.");return}if(password!==confirm){setError("Passwords do not match.");return}try{setBusy(true);setError("");await api.post("/auth/register",{name:name.trim(),email:email.trim(),password});navigation.replace("Verify",{email:email.trim()})}catch(e){setError(e.response?.data?.detail||"Could not create account.")}finally{setBusy(false)}}return <Screen centered><Text style={{color:colors.cyan,fontWeight:"900",letterSpacing:1.4,marginBottom:8}}>STUDENT REGISTRATION</Text><Title>Join Code Ready</Title><Muted>Create your account, verify your email, then choose CSI141 or CSI161.</Muted><Input placeholder="Full name" value={name} onChangeText={setName}/><Input placeholder="Email" autoCapitalize="none" keyboardType="email-address" value={email} onChangeText={setEmail}/><PasswordInput placeholder="Password (8+ characters)" value={password} onChangeText={setPassword}/><Text style={{color:password.length>=8?colors.success:colors.muted,fontSize:12,marginTop:7}}>At least 8 characters</Text><PasswordInput placeholder="Confirm password" value={confirm} onChangeText={setConfirm}/><ErrorText message={error}/><Button title="CREATE ACCOUNT" onPress={submit} loading={busy}/></Screen>}