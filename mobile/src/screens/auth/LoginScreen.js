import React,{useState}from"react";import{Pressable,Text}from"react-native";import Screen from"../../components/Screen";import{Button,ErrorText,Input,Muted,Title}from"../../components/UI";import{useAuth}from"../../context/AuthContext";import{colors}from"../../theme";export default function LoginScreen({navigation}){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[busy,setBusy]=useState(false);const[error,setError]=useState("");const{login}=useAuth();async function submit(){try{setBusy(true);setError("");await login(email.trim(),password)}catch(e){setError(e.response?.data?.detail||"Could not sign in.")}finally{setBusy(false)}}return <Screen centered><Text style={{color:colors.cyan,fontWeight:"900",letterSpacing:2,marginBottom:8}}>CODE READY TUTORS</Text><Title>Welcome back</Title><Muted>Sign in to continue learning.</Muted><Input placeholder="Email" autoCapitalize="none" keyboardType="email-address" value={email} onChangeText={setEmail}/><Input placeholder="Password" secureTextEntry value={password} onChangeText={setPassword}/><ErrorText message={error}/><Button title="SIGN IN" onPress={submit} loading={busy}/><Pressable onPress={()=>navigation.navigate("Register")}><Text style={{color:colors.cyan,textAlign:"center",marginTop:20}}>Create student account</Text></Pressable><Button title="FORGOT PASSWORD?" secondary onPress={()=>navigation.navigate("ForgotPassword")}/></Screen>}