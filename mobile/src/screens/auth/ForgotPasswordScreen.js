import React,{useState}from"react";import Screen from"../../components/Screen";import{Button,ErrorText,Input,Muted,Title}from"../../components/UI";import{api}from"../../api/client";export default function ForgotPasswordScreen({navigation}){const[email,setEmail]=useState("");const[error,setError]=useState("");const[busy,setBusy]=useState(false);async function submit(){try{setBusy(true);setError("");await api.post("/auth/forgot-password",{email});navigation.navigate("ResetPassword",{email})}catch(e){setError(e.response?.data?.detail||"Could not start password reset.")}finally{setBusy(false)}}return <Screen><Title>Reset password</Title><Muted>Enter the email used for your CODE READY TUTORS account.</Muted><Input placeholder="Email" autoCapitalize="none" keyboardType="email-address" value={email} onChangeText={setEmail}/><ErrorText message={error}/><Button title="SEND RESET CODE" loading={busy} onPress={submit}/></Screen>}