import React,{useState}from"react";import{Text}from"react-native";import Screen from"../../components/Screen";import PasswordInput from"../../components/PasswordInput";import{Button,ErrorText,Input,Muted,Title}from"../../components/UI";import{api}from"../../api/client";import{colors}from"../../theme";export default function ResetPasswordScreen({route,navigation}){const email=route.params?.email||"";const[code,setCode]=useState("");const[password,setPassword]=useState("");const[confirm,setConfirm]=useState("");const[error,setError]=useState("");async function submit(){if(password.length<8)return setError("Password must be at least 8 characters.");if(password!==confirm)return setError("Passwords do not match.");try{await api.post("/auth/reset-password",{email,code,new_password:password});navigation.reset({index:0,routes:[{name:"Login"}]})}catch(e){setError(e.response?.data?.detail||"Could not reset password.")}}return <Screen centered><Title>Choose new password</Title><Muted>Enter the 6-digit reset code.</Muted><Input placeholder="000000" keyboardType="number-pad" maxLength={6} value={code} onChangeText={setCode}/><PasswordInput placeholder="New password (8+ characters)" value={password} onChangeText={setPassword}/><Text style={{color:password.length>=8?colors.success:colors.muted,fontSize:12,marginTop:7}}>At least 8 characters</Text><PasswordInput placeholder="Confirm password" value={confirm} onChangeText={setConfirm}/><ErrorText message={error}/><Button title="UPDATE PASSWORD" onPress={submit}/></Screen>}