import React,{useState}from"react";import Screen from"../../components/Screen";import{Button,ErrorText,Input,Muted,Title}from"../../components/UI";import{api}from"../../api/client";export default function ResetPasswordScreen({route,navigation}){const email=route.params?.email||"";const[code,setCode]=useState("");const[password,setPassword]=useState("");const[confirm,setConfirm]=useState("");const[error,setError]=useState("");async function submit(){if(password!==confirm)return setError("Passwords do not match.");try{await api.post("/auth/reset-password",{email,code,new_password:password});navigation.reset({index:0,routes:[{name:"Login"}]})}catch(e){setError(e.response?.data?.detail||"Could not reset password.")}}return <Screen><Title>Choose new password</Title><Muted>Enter the 6-digit reset code.</Muted><Input placeholder="000000" keyboardType="number-pad" maxLength={6} value={code} onChangeText={setCode}/><Input placeholder="New password" secureTextEntry value={password} onChangeText={setPassword}/><Input placeholder="Confirm password" secureTextEntry value={confirm} onChangeText={setConfirm}/><ErrorText message={error}/><Button title="UPDATE PASSWORD" onPress={submit}/></Screen>}