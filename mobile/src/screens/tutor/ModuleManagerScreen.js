import React,{useCallback,useState}from"react";import{Pressable,Text}from"react-native";import{useFocusEffect}from"@react-navigation/native";import Screen from"../../components/Screen";import{Card,Muted,Title}from"../../components/UI";import{api}from"../../api/client";import{colors}from"../../theme";export default function ModuleManagerScreen({navigation}){const[items,setItems]=useState([]);useFocusEffect(useCallback(()=>{api.get("/modules/admin/all").then(r=>setItems(r.data))},[]));return <Screen><Title>Modules & Pricing</Title>{items.map(m=><Pressable key={m.id} onPress={()=>navigation.navigate("ModuleSettings",{module:m})}><Card><Text style={{color:colors.cyan,fontSize:20,fontWeight:"900"}}>{m.code}</Text><Muted>{m.name}</Muted><Text style={{color:colors.white,marginTop:8}}>Group P{m.group_price} · One-on-one P{m.one_on_one_price}</Text><Muted>{m.active?"Active":"Inactive"}</Muted></Card></Pressable>)}</Screen>