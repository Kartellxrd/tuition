import React,{useEffect,useState}from"react";import{Text}from"react-native";import Screen from"../../components/Screen";import{Card,Muted,Title}from"../../components/UI";import{api}from"../../api/client";import{colors}from"../../theme";export default function QuizResultsScreen({route}){const{quiz}=route.params;const[rows,setRows]=useState([]);useEffect(()=>{api.get(`/quizzes/admin/${quiz.id}/results`).then(r=>setRows(r.data))},[quiz.id]);return <Screen><Title>{quiz.title}</Title><Muted>Student results</Muted>{rows.length===0?<Muted>No attempts yet.</Muted>:rows.map(r=><Card key={r.attempt_id}><Text style={{color:colors.white,fontWeight:"900"}}>{r.student_name}</Text><Muted>{r.student_email}</Muted><Text style={{color:colors.cyan,fontSize:22,fontWeight:"900"}}>{r.percentage}% · {r.score}/{r.total}</Text></Card>)}</Screen>}