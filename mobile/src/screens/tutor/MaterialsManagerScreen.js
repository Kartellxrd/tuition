import React,{useEffect,useState}from"react";import{Pressable,Text}from"react-native";import Screen from"../../components/Screen";import{Card,Muted,Title}from"../../components/UI";import{api}from"../../api/client";import{colors}from"../../theme";export default function MaterialsManagerScreen({navigation}){const[modules,setModules]=useState([]);useEffect(()=>{api.get("/modules/admin/all").then(r=>setModules(r.data))},[]);return <Screen><Title>Materials</Title><Muted>Select a module to manage its protected learning files.</Muted>{modules.map(m=><Pressable key={m.id} onPress={()=>navigation.navigate("ModuleMaterials",{module:m})}><Card><Text style={{color:colors.cyan,fontSize:20,fontWeight:"900"}}>{m.code}</Text><Muted>{m.name}</Muted></Card></Pressable>)}</Screen>}