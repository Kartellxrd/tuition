import React from"react";import{Pressable,Text}from"react-native";import Screen from"../../components/Screen";import{Card,Muted,Title}from"../../components/UI";import{colors}from"../../theme";export default function ContentScreen({navigation}){const items=[["Modules & Pricing","ModuleManager"],["Materials","MaterialsManager"],["Quizzes","QuizManager"],["Announcements","AnnouncementsManager"]];return <Screen><Title>Content</Title><Muted>Manage everything students learn from one place.</Muted>{items.map(([label,target])=><Pressable key={target} onPress={()=>navigation.navigate(target)}><Card><Text style={{color:colors.cyan,fontSize:20,fontWeight:"900"}}>{label}</Text><Muted>Open management tools →</Muted></Card></Pressable>)}</Screen>}