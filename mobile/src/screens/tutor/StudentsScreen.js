import React,{useEffect,useState}from"react";import{Text}from"react-native";import Screen from"../../components/Screen";import{Card,Muted,Title}from"../../components/UI";import{api}from"../../api/client";import{colors}from"../../theme";export default function StudentsScreen(){const[rows,setRows]=useState([]);useEffect(()=>{api.get("/enrollments/admin/students").then(r=>setRows(r.data))},[]);return <Screen><Title>Students</Title>{rows.map(x=><Card key={x.enrollment_id}><Text style={{color:colors.white,fontWeight:"900",fontSize:18}}>{x.name}</Text><Muted>{x.email}</Muted><Text style={{color:colors.cyan,fontWeight:"800",marginTop:8}}>{x.tier.replace("_"," ")} · {x.status}</Text></Card>)}</Screen>}