import React,{useEffect,useState}from"react";import{Text}from"react-native";import Screen from"../../components/Screen";import{Card,Muted,Title}from"../../components/UI";import{api}from"../../api/client";import{colors}from"../../theme";export default function TutorScheduleScreen(){const[items,setItems]=useState([]);useEffect(()=>{api.get("/sessions/admin").then(r=>setItems(r.data))},[]);return <Screen><Title>Schedule Manager</Title>{items.map(x=><Card key={x.id}><Text style={{color:colors.cyan,fontWeight:"900"}}>{x.title}</Text><Text style={{color:colors.white}}>{new Date(x.start_at).toLocaleString()}</Text><Muted>{x.tier.replace("_"," ")} · {x.status}</Muted></Card>)}</Screen>}