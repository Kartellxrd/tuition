import React,{useEffect,useState}from"react";import{Text}from"react-native";import Screen from"../../components/Screen";import{Card,Muted,Title}from"../../components/UI";import{api}from"../../api/client";import{colors}from"../../theme";export default function QuizManagerScreen(){const[modules,setModules]=useState([]);useEffect(()=>{api.get("/modules/admin/all").then(r=>setModules(r.data))},[]);return <Screen><Title>Quiz Manager</Title><Muted>Create, publish and review results by module.</Muted>{modules.map(m=><Card key={m.id}><Text style={{color:colors.cyan,fontSize:20,fontWeight:"900"}}>{m.code}</Text><Muted>{m.name}</Muted></Card>)}</Screen>}