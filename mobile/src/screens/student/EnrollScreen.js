import React,{useState}from"react";import{Alert,Text}from"react-native";import Screen from"../../components/Screen";import{Button,Card,ErrorText,Input,Muted,Title}from"../../components/UI";import{api}from"../../api/client";import{colors}from"../../theme";export default function EnrollScreen({route,navigation}){const m=route.params.module;const[tier,setTier]=useState("GROUP");const[reference,setReference]=useState("");const[error,setError]=useState("");return <Screen><Title>{m.code}</Title><Muted>{m.name}</Muted><Card><Text style={{color:colors.white,fontWeight:"800"}}>Select tuition type</Text><Button title={`GROUP · P${m.group_price}`} secondary={tier!=="GROUP"} onPress={()=>setTier("GROUP")}/><Button title={`ONE-ON-ONE · P${m.one_on_one_price}`} secondary={tier!=="ONE_ON_ONE"} onPress={()=>setTier("ONE_ON_ONE")}/></Card><Card><Text style={{color:colors.white,fontWeight:"800"}}>Payment proof</Text><Muted>Enter your payment reference. Proof upload is connected in the next native document-picker step.</Muted><Input placeholder="Payment reference" value={reference} onChangeText={setReference}/><ErrorText message={error}/><Button title="CONTINUE TO PROOF" onPress={()=>{if(reference.trim().length<2)setError("Enter your payment reference.");else Alert.alert("Payment proof","File picker is being wired to the secure upload endpoint.")}}/></Card></Screen>}