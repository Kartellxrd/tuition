import React,{useEffect,useState}from"react";import{Text}from"react-native";import Screen from"../../components/Screen";import{Card,Muted,Title}from"../../components/UI";import{api}from"../../api/client";import{colors}from"../../theme";export default function QuizAttemptsScreen({route}){const{quiz}=route.params;const[items,setItems]=useState([]);useEffect(()=>{api.get(`/quizzes/${quiz.id}/attempts/me`).then(r=>setItems(r.data))},[quiz.id]);return <Screen><Title>{quiz.title}</Title><Muted>Your previous attempts</Muted>{items.length===0?<Muted>No attempts yet.</Muted>:items.map(a=><Card key={a.id}><Text style={{color:colors.cyan,fontSize:24,fontWeight:"900"}}>{a.percentage}%</Text><Muted>{a.score}/{a.total} marks · {new Date(a.submitted_at).toLocaleString()}</Muted></Card>)}</Screen>}
