import React,{useEffect,useState}from"react";import{Text}from"react-native";import Screen from"../../components/Screen";import{Card,Muted,Title}from"../../components/UI";import{api}from"../../api/client";import{colors}from"../../theme";export default function ScheduleScreen(){const[items,setItems]=useState([]);useEffect(()=>{api.get("/sessions/me/upcoming").then(r=>setItems(r.data))},[]);return <Screen><Title>Schedule</Title>{items.length===0?<Muted>No upcoming sessions.</Muted>:items.map(x=><Card key={x.id}><Text style={{color:colors.cyan,fontWeight:"900"}}>{x.title}</Text><Text style={{color:colors.white,marginTop:6}}>{new Date(x.start_at).toLocaleString()}</Text><Muted>{x.mode.replace("_"," ")}{x.location?` · ${x.location}`:""}</Muted></Card>)}</Screen>}