import React,{useEffect,useState}from"react";import{Pressable,Text}from"react-native";import Screen from"../../components/Screen";import{Button,Card,Muted,Title}from"../../components/UI";import{api}from"../../api/client";import{colors}from"../../theme";export default function QuizzesScreen({route,navigation}){const{moduleId}=route.params;const[items,setItems]=useState([]);useEffect(()=>{api.get(`/quizzes/module/${moduleId}`).then(r=>setItems(r.data))},[moduleId]);return <Screen><Title>Quizzes</Title>{items.length===0?<Muted>No published quizzes yet.</Muted>:items.map(q=><Pressable key={q.id} onPress={()=>navigation.navigate("TakeQuiz",{quizId:q.id})}><Card><Text style={{color:colors.cyan,fontSize:19,fontWeight:"900"}}>{q.title}</Text><Muted>Tap to begin</Muted></Card></Pressable>)}</Screen>}