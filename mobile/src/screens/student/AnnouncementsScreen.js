import React,{useEffect,useState}from"react";import{Text}from"react-native";import Screen from"../../components/Screen";import{Card,Muted,Title}from"../../components/UI";import{api}from"../../api/client";import{colors}from"../../theme";export default function AnnouncementsScreen({route}){const{moduleId}=route.params;const[items,setItems]=useState([]);useEffect(()=>{api.get(`/announcements/${moduleId}`).then(r=>setItems(r.data))},[moduleId]);return <Screen><Title>Announcements</Title>{items.length===0?<Muted>No announcements yet.</Muted>:items.map(x=><Card key={x.id}><Text style={{color:x.important?colors.cyan:colors.white,fontWeight:"900",fontSize:18}}>{x.important?"IMPORTANT · ":""}{x.title}</Text><Muted>{x.message}</Muted></Card>)}</Screen>}