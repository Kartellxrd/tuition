import React from"react";import{Text}from"react-native";import Screen from"../../components/Screen";import{Button,Card,Muted,Title}from"../../components/UI";import{colors}from"../../theme";export default function QuizResultScreen({route,navigation}){const{result,title}=route.params;return <Screen><Muted>QUIZ COMPLETE</Muted><Title>{title}</Title><Card><Text style={{color:colors.cyan,fontSize:54,fontWeight:"900"}}>{result.percentage}%</Text><Text style={{color:colors.white,fontSize:20,fontWeight:"800"}}>{result.score} / {result.total} marks</Text></Card><Button title="BACK TO MY LEARNING" onPress={()=>navigation.popToTop()}/></Screen>}