import React from"react";import{Pressable,Text}from"react-native";import Screen from"../../components/Screen";import{Card,Muted,Title}from"../../components/UI";import{colors}from"../../theme";export default function ModuleHubScreen({route,navigation}){const e=route.params.enrollment;const links=[["Materials","Materials"],["Quizzes","Quizzes"],["Announcements","Announcements"]];return <Screen><Muted>ACTIVE LEARNING</Muted><Title>Your module</Title><Muted>{e.tier.replace("_"," ")} tuition</Muted>{links.map(([label,target])=><Pressable key={target} onPress={()=>navigation.navigate(target,{moduleId:e.module_id})}><Card><Text style={{color:colors.cyan,fontSize:21,fontWeight:"900"}}>{label}</Text><Muted>Open {label.toLowerCase()}</Muted></Card></Pressable>)}</Screen>}