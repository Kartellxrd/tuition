import React from"react";import{Text}from"react-native";import Screen from"../components/Screen";import{Button,Card,Muted,Title}from"../components/UI";import{useAuth}from"../context/AuthContext";import{colors}from"../theme";export default function ProfileScreen(){const{user,logout}=useAuth();return <Screen><Title>Profile</Title><Card><Text style={{color:colors.white,fontSize:20,fontWeight:"900"}}>{user?.name}</Text><Muted>{user?.email}</Muted><Text style={{color:colors.cyan,fontWeight:"800",marginTop:12}}>{user?.role}</Text></Card><Button title="SIGN OUT" secondary onPress={logout}/></Screen>}