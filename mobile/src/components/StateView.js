import React from"react";import{ActivityIndicator,StyleSheet,Text,View}from"react-native";import{colors}from"../theme";export default function StateView({loading,error,empty,children}){if(loading)return <View style={s.box}><ActivityIndicator color={colors.cyan}/><Text style={s.text}>Loading…</Text></View>;if(error)return <View style={s.box}><Text style={s.error}>{error}</Text></View>;if(empty)return <View style={s.box}><Text style={s.text}>Nothing here yet.</Text></View>;return children}const s=StyleSheet.create({box:{padding:30,alignItems:"center"},text:{color:colors.muted,marginTop:10},error:{color:colors.danger,textAlign:"center"}});