import React from "react";import {SafeAreaView,ScrollView,StyleSheet} from "react-native";import {colors} from "../theme";export default function Screen({children,scroll=true}){const body=scroll?<ScrollView contentContainerStyle={s.body}>{children}</ScrollView>:children;return <SafeAreaView style={s.safe}>{body}</SafeAreaView>}const s=StyleSheet.create({safe:{flex:1,backgroundColor:colors.background},body:{padding:20,paddingBottom:40}});