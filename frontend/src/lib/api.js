import axios from "axios";export const api=axios.create({baseURL:process.env.NEXT_PUBLIC_API_URL||"http://localhost:8000/api/v1",timeout:15000});export function setToken(token){if(typeof window!=="undefined"){token?localStorage.setItem("crt_token",token):localStorage.removeItem("crt_token")}}api.interceptors.request.use(config=>{if(typeof window!=="undefined"){const token=localStorage.getItem("crt_token");if(token)config.headers.Authorization=`Bearer ${token}`}return config});